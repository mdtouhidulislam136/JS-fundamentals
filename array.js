/// if we want to use more value in js we have to use array

// number array
var friendAge = [12, 13, 14, 15, 16, 17]

// string array
var fruits = ["apple", "orange", "Mango", "grap", "Malta"]

// odd number array

var oddNumber = [1, 3, 5, 7, 9, 11, 13]

var vowels = ['a', 'e', 'i', 'o', 'u']

// check array length

console.log(vowels.length)
console.log( oddNumber)